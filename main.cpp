#include <iostream>
#include "lib.h"
using namespace std;

int main() {
int i;
int a;
int max;
int numeri[i];
cin >> i;
for (a=0; a<i; a++)
    {
    	cin >> numeri[a];
    }
max=funzione(numeri, i);
cout << max;
return 0;
}
