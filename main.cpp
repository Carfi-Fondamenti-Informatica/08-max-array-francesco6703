#include <iostream>
#include "lib.h"

int main() {
int i;
int a;
int numeri[i];
std::cin >> i;
    for (int a=0; a<i; a++)
    {
        std::cin >> numeri[a];
    }

std::cout << funzione(numeri, i);
return 0;
}
