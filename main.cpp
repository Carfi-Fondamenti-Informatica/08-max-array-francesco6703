#include <iostream>
#include "lib.h"

int main() {
int i;
int a;
int numeri[i];
std::cin >> i;
int x=1, max=numeri[0];
    for (int a=0; a<i; a++)
    {
        std::cin >> numeri[a];
    }

std::cout << funzione(numeri, i);
return 0;
}
