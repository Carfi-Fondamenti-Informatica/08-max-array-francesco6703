#include <iostream>
#include "lib.h";

int main() {
int i;
int a;
int max;
int numeri[i];
std:: cin >> i;
for (a=0; a<i; a++)
    {
    	std:: cin >> numeri[a];
    }
max=funzione(numeri, i);
std:: cout << max;
return 0;
}
